import QtQuick 2.0

Item {
    // Use only color names defined here!
    // Color values are dynamically changed!
    property color keyColor: "#000000"
    property color bgColor: "#ffffff"
    property color titleColor: "#999999"
}
